# Django 核心框架
Django==6.0.6

# API 框架
django-ninja==1.6.2
django-ninja-extra==0.31.5
django-ninja-jwt==5.4.4

# 測試框架
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-django==4.12.0

# CORS 支持
django-cors-headers==4.9.0

# WebSocket 和實時通信
channels[daphne]==4.3.2
channels-redis==4.3.0

# 數據庫適配器
psycopg[binary]==3.3.4

# AI 整合 — Pydantic AI 接 NVIDIA NIM（OpenAI 相容端點）或 Gemini（Google 原生 SDK）
pydantic-ai-slim[openai,google]==2.0.0
